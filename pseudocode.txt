PROMPT FOR LENGTH  
    - if between 8-50 ask for special characters
    - else prompt for special characters
        - prompt for numeric characters
        - prompt for upper case letters
        - prompt for lowercase letters 
    - else if ask to reenter length 
        - prompt for upper case letters
        - prompt for lowercase letters

ALL FOUR OPTIONS 
    - else if special characters, numbers, uppercase & lowercase 
        - return password 
 
THREE OPTIONS 

    - else if special characters, numbers, uppercase
        - return password 
    - else if special characters, numbers, lowercase 
        - return password
    - else if numbers, uppercase, lowercase 
        - return password
    - else if numbers, uppercase, lowercase 
        - return password
 
TWO OPTIONS 

    - else if numbers, uppercase
        - return password
    - else if numbers, lowercase 
        - return password 
    - else if numbers, special characters 
        - return password 
    - else if uppercase, lowercase 
        - return password 
    - else if special characters, uppercase 
        - return password 
    - else if special characters, lowercase 
        - return password 
   
ONE OPTION 

    - else if lowercase 
        - return password
    - else if uppercase 
        - return password 
    - else if numbers
        - return password 
    - else if special character 
        - return password 

GENERATE PASSWORD IN BOX ON PAGE 
    




  
